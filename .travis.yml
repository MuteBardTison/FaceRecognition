language: cpp

compiler:
  - gcc
  - clang

before_script:
  - sudo apt-get install libopencv-dev
  - sudo apt-get install libboost-all-dev
  - mkdir build output
  - cd build
  - cmake ..

script: make
